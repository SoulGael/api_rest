export default {
  phone_number: [lengthNumber], 
  job: [lengthNumber]
}

function lengthNumber(value) {
  // tener acceso al campo phone number
  // si cumple el length es 5
  // si no cumple el length se realiza un throw, rechazado
  // si cumple el length solo funciona.
}